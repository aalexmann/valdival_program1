1. Open up the command line
2. Make sure gcc is installed, test using the command: gcc -v, and if there is a verion, then it is installed.
3. 

The rest of this project will be submitted as a revised version.